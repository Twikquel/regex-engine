# Regex Engine

A simple regex engine in c++ based on the formal definition of Regular Expressions.

To compile use:
```bash
make
```

To compile and run tests use:
```bash
make test
```

To run a simple interactive interface use:
```bash
make run
```

